<?php 
    class Person{
        public $isAlive = true;
        public $lastname;
        public $age; 
        
        public function __construct($lastname,$age){
                $this->lastname = $lastname;
                $this->age = $age;
                }
        }
        
        class Kid extends Person {
        
        } 
        
        $student = new Person("borink", 15);
	    $kid = new Kid();
        
        echo($student->isAlive);
        echo($student->age);

        if (property_exists($kid,"isAlive")) {
          echo "I'm alive!";
        }
?>